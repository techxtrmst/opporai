# =============================================================================
# Production Environment & Deployment Secrets — EXAMPLE
# =============================================================================
# Copy this file to .env.production and fill in real values:
#   cp .env.production.example .env.production
#
# Then run: pnpm setup:secrets
# Then push to main to deploy.
# =============================================================================

# --- Deployment / Server Config ---
DEPLOY_HOST=your.server.ip.address
DEPLOY_SSH_PORT=22
DEPLOY_USER=your_ssh_username
DEPLOY_APP_DIR=your_app_directory
# Path to the SSH private key file (used by setup script to read & upload)
# Generate one: ssh-keygen -t ed25519 -f ~/.ssh/myapp_deploy -N "" -C "ci-deploy"
# Then add the .pub key to server's ~/.ssh/authorized_keys
DEPLOY_SSH_KEY_PATH=~/.ssh/myapp_deploy

# --- App Runtime Env Vars (injected into the deployed app) ---
NODE_ENV=production
# DATABASE_URL=postgresql://user:pass@host:5432/db
# NEXT_PUBLIC_API_URL=https://api.example.com
